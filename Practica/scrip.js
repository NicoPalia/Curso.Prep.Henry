function suma(x, y) {
    // "x" e "y" son números
    // Suma "x" e "y" juntos y devuelve el valor
    // Tu código:
    var x = Number
    var y = Number
    var suma = x + y 
    return suma
    }

suma(10,1)
